<?xml version="1.0" encoding="utf-8"?>
<!--
  Language: XML
  $Author$
  $Date$
  $Revision$
  $Copyright: [2010-2014] The CoSMo Company, All Rights Reserved $
-->
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
    "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<section>
  <title>Usage</title>
  <para>Once the BioPreDyn prototype is installed, it can be used to run
numerical experiments encoded as SEDML files. Simply open a shell or a
command prompt, navigate to the folder where the prototype is installed, and
type the following commands:</para>
  <programlisting>
  cd build/install/bin
  </programlisting>
  <para>This folder contains a biopredyn.py file which can be used to run the
prototype. To do so, one can use the following syntax:
  </para>
  <programlisting>
python biopredyn.py [options]
  </programlisting>
  <para>Depending on the option(s) chosen by the user, various operations can be
done. Valid options are listed below:</para>
  <itemizedlist>
    <listitem>
      <para><literal>--sedml [path/to/file.xml]</literal>: opens the input SEDML
file, executes the tasks it contains and process its outputs. Graphical outputs
are displayed, if any; path/to/file.xml must point to a valid SEDML file.</para>
    </listitem>
    <listitem>
      <para><literal>--output [path/to/output.csv]</literal>: write the result
of a numerical experiment (if any) to the input location as a CSV file. This
option should be used only when the opened SEDML file contains one or more
“report” elements.</para>
    </listitem>
    <listitem>
      <para><literal>--output [path/to/output.xml]</literal>: identical to the
previous one, except that the result is exported as a NuML file instead.</para>
    </listitem>
    <listitem>
      <para><literal>--csv [path/to/file.csv]</literal>: opens the input CSV
file and plot its content; path/to/file.csv must point to a valid CSV file.
      </para>
    </listitem>
    <listitem>
      <para><literal>--numl [path/to/file.xml]</literal>: opens the input NuML
file and plot its content; path/to/file.xml must point to a valid NuML file.
      </para>
    </listitem>
  </itemizedlist>
</section>